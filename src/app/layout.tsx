import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "TagQuest - HTML Structure Training",
  description:
    "Learn HTML tags and document structure through game-like exercises. Interactive training for beginners.",
  keywords: ["HTML", "learning", "structure", "tags", "beginner", "interactive"],
  openGraph: {
    title: "TagQuest - HTML Structure Training",
    description:
      "Learn HTML tags and document structure through game-like exercises.",
    type: "website",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="ko">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
